Place small test images in this directory to exercise the frame captioning pipeline.

Recommended filenames:

- `frame_001.png`
- `frame_002.png`

The v1.0 `filename_stub` backend does not inspect image content. It only uses the
filename to generate placeholder captions, so tiny placeholder PNG files are
sufficient for pipeline testing.
